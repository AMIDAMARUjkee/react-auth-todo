import './Header.scss';

const Header = () => {
  return (
    <header className="header">
      <h1 className="header__title">To-Do (React, Redux, FireBase)</h1>
    </header>
  );
};

export default Header;
